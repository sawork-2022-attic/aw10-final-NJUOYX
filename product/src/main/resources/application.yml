server:
  port: 8002
eureka:
  instance:
    instance-id: product
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8000/eureka
spring:
  application:
    name: product